---
import { GitBranch, Globe } from '@lucide/astro';
import BackToTop from '../components/BackToTop.astro';
import SiteFooter from '../components/SiteFooter.astro';
import SiteHeader from '../components/SiteHeader.astro';
import Layout from '../layouts/Layout.astro';
import { developers } from '../data/site';
---

<Layout title="Developers" description="Meet the people behind Chain Reaction MAX.">
  <SiteHeader />

  <div class="page-shell">
    <main class="container dev-page" data-pagefind-body>
      <section class="section dev-hero" data-reveal>
        <span class="eyebrow">Meet the devs</span>
        <h1 class="page-title">The people shaping each release.</h1>
        <p class="lede">
          Built and maintained together, from core gameplay logic to release polish.
        </p>
      </section>

      <section class="section-tight dev-showcase">
        {
          developers.map((dev, index) => {
            const website = dev.links.website;
            const githubHandle = dev.links.github?.split('/').filter(Boolean).pop();
            return (
              <article class="dev-card" data-reveal style={`--card-index:${index};`}>
                <div class="dev-core">
                  <div class="dev-avatar-wrapper">
                    {dev.avatar ? (
                      <img src={dev.avatar} alt={dev.name} class="dev-avatar" />
                    ) : (
                      <span class="dev-initials">{dev.initials}</span>
                    )}
                  </div>
                  <div>
                    <h2>{dev.name}</h2>
                    {githubHandle && <p class="dev-handle">@{githubHandle}</p>}
                  </div>
                </div>
                <div class="dev-links">
                  {dev.links.github && (
                    <a href={dev.links.github} target="_blank" rel="noreferrer">
                      <GitBranch size={16} /> GitHub
                    </a>
                  )}
                  {website && (
                    <a href={website} target="_blank" rel="noreferrer">
                      <Globe size={16} /> Website
                    </a>
                  )}
                </div>
              </article>
            );
          })
        }
      </section>
    </main>
  </div>

  <BackToTop />
  <SiteFooter />
</Layout>

<style>
  .dev-page {
    display: grid;
    gap: 0;
  }

  .dev-showcase {
    display: grid;
    gap: 1.5rem;
    align-items: start;
    grid-template-columns: 1fr;
  }

  @media (min-width: 640px) {
    .dev-showcase {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (min-width: 980px) {
    .dev-showcase {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  .dev-card {
    position: relative;
    display: grid;
    align-content: start;
    border: 1px solid var(--line-soft);
    border-radius: 20px;
    padding: 1.75rem;
    background: var(--bg-panel);
    backdrop-filter: blur(16px);
    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    overflow: hidden;
    z-index: 1;
  }

  .dev-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100%;
    background: radial-gradient(
      circle at calc(85% - var(--card-index) * 15%) 20%,
      var(--line),
      transparent 55%
    );
    z-index: 0;
    pointer-events: none;
    transition: opacity 0.4s ease;
    opacity: 0.35;
  }

  .dev-card:hover {
    transform: translateY(-8px);
    border-color: var(--accent-2);
    box-shadow: 0 12px 30px rgba(62, 194, 255, 0.15), 0 0 2px rgba(62, 194, 255, 0.3);
  }

  :global([data-theme="dark"]) .dev-card:hover {
    box-shadow: 0 12px 30px rgba(62, 194, 255, 0.22), 0 0 2px rgba(62, 194, 255, 0.4);
  }

  .dev-core {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 1.1rem;
    align-items: center;
    margin-bottom: 0.72rem;
    z-index: 2;
  }

  .dev-avatar-wrapper {
    position: relative;
    width: 3.75rem;
    height: 3.75rem;
    border-radius: 50%;
    padding: 3px;
    background: linear-gradient(135deg, var(--accent), var(--accent-2));
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    box-shadow: 0 3px 12px rgba(0, 0, 0, 0.12);
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .dev-card:hover .dev-avatar-wrapper {
    transform: scale(1.08) rotate(5deg);
  }

  .dev-avatar {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 50%;
    background-color: var(--bg);
    border: 2px solid var(--bg);
  }

  .dev-initials {
    display: inline-grid;
    place-items: center;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: var(--bg);
    font-family: 'Fraunces', 'Outfit', serif;
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text);
    border: 2px solid var(--bg);
  }

  .dev-card h2 {
    margin: 0;
    font-family: 'Fraunces', 'Outfit', serif;
    font-size: 1.52rem;
    line-height: 1.06;
    color: var(--text);
  }

  .dev-handle {
    margin: 0.3rem 0 0;
    color: var(--accent);
    font-size: 0.84rem;
    letter-spacing: 0.07em;
    font-weight: 600;
    text-transform: uppercase;
  }

  .dev-card p {
    margin: 0;
    color: var(--text-muted);
  }

  .dev-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
    margin-top: 0.95rem;
    padding-top: 0.82rem;
    border-top: 1px solid var(--line-soft);
    z-index: 2;
  }

  .dev-links a {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    color: var(--text-muted);
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.2s ease, transform 0.2s ease;
  }

  .dev-links a:hover {
    color: var(--accent);
    transform: translateX(2px);
  }
</style>
